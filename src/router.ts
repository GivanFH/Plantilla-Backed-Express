//importar instancia del router
import { Router } from "express"
import { param } from "express-validator";
import { handleInputErrors } from "./middleware";

const router = Router()

//Routing




export default router